# LoanOver Brand — conventions source

The uploaded `README.md` in `ds-bundle/` IS the conventions document (hand-authored layout — no
converter, no `readmeHeader` stitching). Edit `ds-bundle/README.md` directly; this file just
records that decision so a future sync doesn't author a second header.

Validated 2026-08-20: every `--lo-*` token named in README and cards exists in
`ds-bundle/tokens/*.css`; every named card path exists under `ds-bundle/components/`.
